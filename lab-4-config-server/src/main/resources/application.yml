---
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/gtoumie/ms-discovery-eureka
          searchPaths: lab-4-config-data
        native:
          searchLocations: classpath:offline-repository/
server:
  port: 8001